pipeline {
     agent any
     stages {
         stage('S3download') {
             steps {
                 
    withAWS(credentials:'hibooks-stage') {
          s3Upload(bucket:"hibooks-files", includePathPattern:'**/*.svg')
                }
            }
        }
    }
}
