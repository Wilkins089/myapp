pipeline {
     agent any
     stages {
         stage('S3download') {
             steps {
                 
    withAWS(credentials:'hibooks-stage') {
          s3Upload(bucket:"hibooks-files", path:'/var/lib/jenkins/workspace/Memod-editor', includePathPattern:'**/*.svg')
                }
            }
        }
    }
}
